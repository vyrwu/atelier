/bin/
/dist/
# Stray root build artifact from `go build ./cmd/atelier` (binary name == repo name).
/atelier
*.test
*.out
result
result-*
.direnv/

# Private planning / scratch docs — kept local, never committed.
FEATURE_REQUESTS.md
PLAN.md
REFACTOR.md
